# Node / package managers
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# Lockfile intentionally excluded — prototype has no locked toolchain yet.
# Remove this line and commit the lockfile once we standardize on npm.
package-lock.json
yarn.lock
pnpm-lock.yaml

# Environment
.env
.env.*
!.env.example

# Supabase local state
supabase/.branches/
supabase/.temp/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
